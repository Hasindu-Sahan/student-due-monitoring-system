# TODO

## Completed / In-progress
- [x] Admin Payments API: extend `/api/admin/payments` response with `category`, `faculty`, `level`.
- [ ] Admin Payments tab: remove old status filter + add new filters (fee type, category, faculty, level, payment status, student-wise).
- [ ] Admin Fee Management tab: remove "Add New Fee" section; keep edit + all fee types table.
- [ ] Verify build/lint/dev run.

## Faculty login seed
- [ ] Add faculty login user seed (`fac001` / `fac@001`) as `User.role = Staff`.
- [ ] Add matching `Admin` profile row for that user.
- [ ] Run seed and verify login works.


