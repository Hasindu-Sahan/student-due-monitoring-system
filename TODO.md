# TODO
- [x] Remove "specific student" option from the Belongs To dropdown in `src/app/admin/add-fee/page.tsx`.
- [ ] Update receiver logic (remove receiver radio / student ID conditional) so fee can only be assigned to faculty/level.
- [ ] Ensure form submission payload no longer sends/depends on `SPECIFIC_STUDENT` / `studentId`.
- [ ] Run TypeScript check / build to confirm no errors.

